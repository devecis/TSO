---
all:
  children:
    deveci_jump:
      hosts:
        jump_server_hostname:
          ansible_host: deveci-jump.local.forwardnetworks.com
          ansible_user: forward
    cisco_ios:
      hosts:
        cisco-ios-device:
          ansible_host: 15.0.0.254
